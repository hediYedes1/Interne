<form method="post" action="{{ path('app_projet_delete', {'idprojet': projet.idprojet}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ projet.idprojet) }}">
    <button class="btn">Delete</button>
</form>
