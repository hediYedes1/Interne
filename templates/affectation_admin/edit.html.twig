{% extends 'base1.html.twig' %}

{% block title %}Edit Affectationhebergement{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow-sm">
                    <div class="card-header bg-primary text-white">
                        <h1 class="h3 mb-0">Modifier la réservation</h1>
                    </div>
                    <div class="card-body p-4">
                        {{ include('affectation_admin/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                    </div>
                    <div class="card-footer bg-light d-flex justify-content-between">
                        <a href="{{ path('app_affectation_admin_index') }}" class="btn btn-outline-secondary">
                            <i class="fas fa-arrow-left me-2"></i>Retour à la liste
                        </a>
                        {{ include('affectation_admin/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
