{{ form_start(form, {'attr': {'class': 'custom-affectation-form'}}) }}

    <div class="form-group">
        {{ form_label(form.datedebut) }}
        {{ form_widget(form.datedebut) }}
        <div class="form-error">{{ form_errors(form.datedebut) }}</div>
    </div>

    <div class="form-group">
        {{ form_label(form.datefin) }}
        {{ form_widget(form.datefin) }}
        <div class="form-error">{{ form_errors(form.datefin) }}</div>
    </div>

    <div class="form-group">
        {{ form_label(form.idhebergement) }}
        {{ form_widget(form.idhebergement) }}
        <div class="form-error">{{ form_errors(form.idhebergement) }}</div>
    </div>

    <div class="form-group">
        {{ form_label(form.idutilisateur) }}
        {{ form_widget(form.idutilisateur) }}
        <div class="form-error">{{ form_errors(form.idutilisateur) }}</div>
    </div>

    <button class="btn">Enregistrer</button>

{{ form_end(form) }}

<style>
    .custom-affectation-form {
        background-color: #fefefe !important;
        padding: 25px !important;
        border-radius: 10px !important;
        box-shadow: 0 0 15px rgba(0, 0, 0, 0.08) !important;
        max-width: 700px !important;
        margin: 40px auto !important;
        font-family: 'Segoe UI', sans-serif !important;
    }

    .form-group {
        margin-bottom: 20px !important;
    }

    .custom-affectation-form label {
        display: block !important;
        margin-bottom: 6px !important;
        font-weight: 600 !important;
        color: #333 !important;
    }

    .custom-affectation-form input,
    .custom-affectation-form select {
        width: 100% !important;
        padding: 10px !important;
        margin-bottom: 5px !important;
        border: 2px solid #3d74b6 !important;
        border-radius: 8px !important;
        font-size: 15px !important;
        box-sizing: border-box !important;
    }

    .form-error {
        color: #d9534f !important;
        font-size: 14px !important;
        margin-top: 4px !important;
    }

    .custom-affectation-form .btn {
        background-color: #3d74b6 !important;
        color: white !important;
        padding: 12px 24px !important;
        border: none !important;
        border-radius: 8px !important;
        font-size: 16px !important;
        cursor: pointer !important;
        transition: background 0.3s ease !important;
    }

    .custom-affectation-form .btn:hover {
        background-color: #2c5f9e !important;
    }
</style>
