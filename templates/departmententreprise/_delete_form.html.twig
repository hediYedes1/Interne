<form method="post" action="{{ path('app_departmententreprise_delete', {'iddepartement': departmententreprise.iddepartement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ departmententreprise.iddepartement) }}">
    <button class="btn">Delete</button>
</form>
