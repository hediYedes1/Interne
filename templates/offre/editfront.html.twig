{% extends 'base.html.twig' %}

{% block title %}Edit Offre{% endblock %}

{% block body %}
    <h1>Edit Offre</h1>

    {{ include('offre/_form.html.twig', {
        'form': form,
        'button_label': 'Update',
        'offre': offre
    }) }}

    <a href="{{ path('front_offre_index') }}">back to list</a>

    {{ include('offre/_delete_form.html.twig', {
        'offre': offre
    }) }}
{% endblock %}
