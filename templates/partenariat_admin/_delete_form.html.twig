<form method="post" action="{{ path('app_partenariat_admin_delete', {'idpartenariat': partenariat.idpartenariat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ partenariat.idpartenariat) }}">
    <button class="btn">Delete</button>
</form>
