{% extends 'base1.html.twig' %}

{% block title %}Détails de l'Entreprise{% endblock %}

{% block css %}
  {{ parent() }}
  <style>
    .container {
      margin-top: 20px;
    }

    .card {
      border-radius: 8px;
      border: 1px solid #ddd;
      margin-bottom: 20px;
    }

    .card-header {
      background-color: #007bff;
      color: white;
      font-size: 1.25rem;
      font-weight: bold;
      border-top-left-radius: 8px;
      border-top-right-radius: 8px;
    }

    .card-body {
      background-color: #f9f9f9;
      padding: 1.5rem;
    }

    .table {
      width: 100%;
      margin-bottom: 1rem;
      background-color: transparent;
      border-collapse: collapse;
    }

    .table th, .table td {
      padding: 12px;
      text-align: left;
      vertical-align: middle;
      border-top: 1px solid #ddd;
    }

    .table th {
      background-color: #f5f5f5;
      color: #333;
    }

    .btn-group {
      display: flex;
      gap: 10px;
      justify-content: flex-start;
    }

    .btn {
      font-size: 1rem;
      padding: 0.75rem 1.25rem;
      border-radius: 5px;
    }

    .btn-secondary {
      background-color: #6c757d;
      border-color: #6c757d;
    }

    .btn-secondary:hover {
      background-color: #5a6268;
      border-color: #545b62;
    }

    .btn-primary {
      background-color: #007bff;
      border-color: #007bff;
    }

    .btn-primary:hover {
      background-color: #0056b3;
      border-color: #0056b3;
    }

    .text-center {
      text-align: center;
    }

    .mb-3 {
      margin-bottom: 1.5rem;
    }

    .actions-container {
      margin-top: 20px;
    }
  </style>
{% endblock %}

{% block body %}
  <div class="container">
    <h1 class="text-center mb-4">Détails de l'Entreprise</h1>

    <div class="card shadow-sm">
      <div class="card-header">
        <h6 class="m-0 font-weight-bold text-white">Informations de l'entreprise</h6>
      </div>
      <div class="card-body">
        <table class="table table-bordered">
          <tbody>
            <tr>
              <th>Identifiant</th>
              <td>{{ entreprise.identreprise }}</td>
            </tr>
            <tr>
              <th>Nom de l'entreprise</th>
              <td>{{ entreprise.nomentreprise }}</td>
            </tr>
            <tr>
              <th>Description</th>
              <td>{{ entreprise.descriptionentreprise }}</td>
            </tr>
            <tr>
              <th>Logo</th>
              <td>
                {% if entreprise.logoentreprise %}
                  <img src="{{ asset('uploads/logos/' ~ entreprise.logoentreprise) }}" alt="Logo de l'entreprise" class="img-fluid" style="max-width: 100px;">
                {% else %}
                  Aucune image
                {% endif %}
              </td>
            </tr>
            <tr>
              <th>URL</th>
              <td>
                <a href="{{ entreprise.urlentreprise }}" target="_blank" class="btn btn-info btn-sm">
                  Voir le site
                </a>
              </td>
            </tr>
            <tr>
              <th>Secteur</th>
              <td>{{ entreprise.secteurentreprise }}</td>
            </tr>
            <tr>
  <th>Branches associées</th>
  <td>
    {% if branches is not empty %}
      <ul class="list-group mb-3">
        {% for branche in branches %}
          <li class="list-group-item">
            📍 <em>{{ branche.localisationbranche }}</em><br>
            📞 {{ branche.contactbranche}}<br>
            👤 Responsable : {{ branche.responsablebranche }}<br>
            👥 Employés : {{ branche.nombreemploye }}<br>
            ✉️ {{ branche.emailbranche }}
          </li>
          <a href="{{ path('app_brancheentreprise_edit', {'idbranche': branche.idbranche}) }}" class="btn btn-primary">
          <i class="fas fa-edit mr-2"></i>Modifier
        </a>
          <div class="mt-3">
        {{ include('entreprise/_delete_form_branche.html.twig') }}
      </div>
        {% endfor %}
      </ul>
    {% else %}
      <span class="text-muted">Aucune branche associée</span>
    {% endif %}
  </td>
</tr>

<tr>
  <th>Départements associés</th>
  <td>
    {% if departments is not empty %}
      <ul class="list-group">
        {% for d in departments %}
          <li class="list-group-item">
         🏢 {{ d.nomdepartement }} <br>
👤 Responsable : {{ d.responsabledepartement }}  <br>
📋 Description :{{ d.descriptiondepartement }}
          </li>

   <a href="{{ path('app_departmententreprise_edit', {'iddepartement': d.iddepartement}) }}" class="btn btn-primary">    
         <i class="fas fa-edit mr-2"></i>Modifier
        </a>
              <div class="mt-3" >>

     {{ include('entreprise/_delete_form_departement.html.twig') }}
           </div>

</div>
   
        {% endfor %}
      </ul>
    {% else %}
      <span class="text-muted">Aucun département associé</span>
    {% endif %}
  </td>
</tr>


          </tbody>
        </table>
      </div>
    </div>

    <div class="actions-container text-center">
      <div class="btn-group">
        <a href="{{ path('app_entreprise_index') }}" class="btn btn-secondary">
          <i class="fas fa-arrow-left mr-2"></i>Retour à la liste
        </a>
        <a href="{{ path('app_entreprise_edit', {'identreprise': entreprise.identreprise}) }}" class="btn btn-primary">
          <i class="fas fa-edit mr-2"></i>Modifier
        </a>
      </div>

      <div class="mt-3">
        {{ include('entreprise/_delete_form.html.twig') }}
      </div>
    </div>
  </div>
{% endblock %}