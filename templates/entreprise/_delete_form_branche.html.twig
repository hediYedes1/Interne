<form method="post" action="{{ path('app_brancheentreprise_delete', {'idbranche': branche.idbranche}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ branche.idbranche) }}">
    <button class="btn">Delete</button>
</form>
