<form method="post" action="{{ path('app_departmententreprise_delete', {'iddepartement': d.iddepartement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ d.iddepartement) }}">
    <button class="btn">Delete</button>
</form>
