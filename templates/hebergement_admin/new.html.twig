{% extends 'base1.html.twig' %}

{% block title %}Nouveau Hébergement{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="display-5">Créer un nouvel hébergement</h1>
        <a href="{{ path('app_hebergement_admin_index') }}" class="btn btn-outline-primary">⬅️ Retour à la liste</a>
    </div>

    <div class="card shadow-sm p-4">
        {{ include('hebergement_admin/_form.html.twig') }}
    </div>
</div>
{% endblock %}
