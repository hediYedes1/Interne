<form method="post" action="{{ path('app_hebergement_admin_delete', {'idhebergement': hebergement.idhebergement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hebergement.idhebergement) }}">
    <button class="btn">Delete</button>
</form>
