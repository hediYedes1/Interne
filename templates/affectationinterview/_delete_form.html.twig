<form method="post" action="{{ path('app_affectationinterview_delete', {'idinterview': affectationinterview.idinterview}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ affectationinterview.idinterview) }}">
    <button class="btn">Supprimer</button>
</form>
