<form method="post" action="{{ path('app_affectationhebergement_delete', {'idhebergement': affectationhebergement.idhebergement.idhebergement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ affectationhebergement.idhebergement.idhebergement) }}">
    <button class="btn">Delete</button>
</form>
