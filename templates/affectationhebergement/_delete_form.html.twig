<form method="post" action="{{ path('app_affectationhebergement_delete', {'idhebergement': affectationhebergement.idhebergement.idhebergement}) }}" 
      onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette réservation ?');" class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ affectationhebergement.idhebergement.idhebergement) }}">
    <button class="btn btn-danger">
        <i class="bi bi-trash me-1"></i> Supprimer
    </button>
</form>